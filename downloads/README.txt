Do not publish development or unsigned installers here.

When a release candidate is approved, upload the final installer to the chosen release storage/CDN and set `downloadUrl` in assets/site.js to that HTTPS URL.
Publish a SHA-256 checksum alongside the installer on the website or release page.
